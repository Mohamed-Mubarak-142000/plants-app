export enum ROUTES {
  HOME = "/",
  CONTACT = "/contact",
  PLANTS = "/plants",
  STANDS = "/stands",
  CATEGORIES = "/categories",
}

export enum AUTH {
  LOGIN = "/login",
  REGISTER = "/register",
}
